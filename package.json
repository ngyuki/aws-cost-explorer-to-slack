{
  "main": "index.js",
  "dependencies": {
    "@slack/webhook": "^5.0.3"
  },
  "devDependencies": {
    "aws-sdk": "^2.679.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
